{
  "name": "safelight-advanced-library-sort",
  "private": true,
  "version": "1.1.1",
  "description": "Lightroom-style Library sorting (camera, lens, focal length, ISO) plus live search and saved Smart Searches for SafeLight.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "install-local": "node scripts/install.mjs",
    "test": "node --experimental-strip-types src/query.test.ts && node --experimental-strip-types src/sort.test.ts"
  },
  "devDependencies": {
    "rolldown": "^1.1.3",
    "typescript": "~5.7.0"
  }
}
